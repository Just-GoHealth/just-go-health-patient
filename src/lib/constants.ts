// set once a visitor has been through the /versions invitation screen, so
// it shows only on their first visit rather than on every load of "/"
export const VERSIONS_SEEN_KEY = "justgo_versions_seen";
